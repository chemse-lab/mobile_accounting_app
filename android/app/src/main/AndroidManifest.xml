<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ===================== أذونات البلوتوث (لطباعة الفواتير) ===================== -->

    <!-- لأجهزة أندرويد 11 وما قبل -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <!-- لأجهزة أندرويد 12+ (API 31+) - النظام الجديد لأذونات البلوتوث -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />

    <!-- مطلوب على بعض الأجهزة القديمة لاكتشاف أجهزة البلوتوث القريبة -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- ===================== أذونات أخرى ===================== -->

    <!-- للوصول للإنترنت (تحديثات مستقبلية، إن وجدت) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- للنسخ الاحتياطي ومشاركة الفواتير PDF -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <!-- يخبر متجر Google Play أن التطبيق يعمل حتى على أجهزة بدون بلوتوث (اختياري) -->
    <uses-feature
        android:name="android.hardware.bluetooth"
        android:required="false" />

    <application
        android:label="تطبيق المحاسبة"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:requestLegacyExternalStorage="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- يخبر Flutter بأن لا حاجة لقيمة versionCode معينة بشكل ديناميكي -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- موفّر الملفات اللازم لمشاركة فواتير PDF والنسخ الاحتياطية -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- مطلوب لـ Android 11+ (queries) حتى يستطيع التطبيق فتح تطبيقات مشاركة أخرى -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="application/pdf" />
        </intent>
    </queries>

</manifest>
